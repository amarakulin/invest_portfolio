import styled from 'styled-components';

export const ShowPassword = styled.div`
	font-weight: normal;
	font-size: 12px;
	line-height: 15px;
	color: #000000;
	position: absolute;
	top: 50%;
	right: 10px;
	transform: translateY(50%);
	cursor: pointer;
`